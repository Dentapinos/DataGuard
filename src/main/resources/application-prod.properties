# =============================================
# ЛОГОРИРОВАНИЕ
# =============================================
# Уровень логирования для продакшна (переопределяет значение из application.properties)
logging.level.com.dentapinos.dataguard=INFO

# =============================================
# APP URLs
# =============================================
app.server.base-url=https://kotitonttu.tech/backups/

# =============================================
# ПОДКЛЮЧЕНИЕ К БАЗАМ ДАННЫХ
# =============================================
# Для продакшна рекомендуется использовать переменные окружения или secret-менеджеры
# Пример конфигурации (закомментирован):
# backup.param.databases[0].database-name=production-db
# backup.param.databases[0].display-name=Production Database
# backup.param.databases[0].url=${PROD_JDBC_URL:jdbc:mysql://localhost:3306/production}
# backup.param.databases[0].username=${PROD_DB_USERNAME:production_user}
# backup.param.databases[0].password=${PROD_DB_PASSWORD:}
backup.param.databases[0].database-name=documents
backup.param.databases[0].display-name=documents
backup.param.databases[0].url=jdbc:mysql://mysql_doc:3306/docmultilang?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
backup.param.databases[0].username=root
backup.param.databases[0].password=root_pass
